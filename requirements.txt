# Sentinel Protocol — Python dependencies
# Install with: pip install -r requirements.txt

# ── Jupyter environment ───────────────────────────────────────────────────────
jupyterlab==4.6.3       # JupyterLab IDE (pulls in jupyter-core, notebook server)
ipykernel==7.3.0        # Python kernel for .ipynb execution
nbformat==5.11.1        # Notebook file format read/write
nbconvert==7.17.1       # Export and execute notebooks (nbconvert --execute)
nbclient==0.11.0        # Programmatic notebook execution engine

# ── Streamlit dashboard ───────────────────────────────────────────────────────
streamlit==1.62.0       # Live mission dashboard (dashboard.py)

# ── IBM watsonx.ai ────────────────────────────────────────────────────────────
ibm-watsonx-ai==1.6.3   # Foundation model client (ibm/granite-4-h-small, chat API)

# ── Credentials / environment ─────────────────────────────────────────────────
python-dotenv==1.2.2    # Loads WATSONX_API_KEY and WATSONX_PROJECT_ID from .env

# ── Data handling ─────────────────────────────────────────────────────────────
pandas==2.3.3           # Tick history table in the dashboard

# ── Anomaly detection ─────────────────────────────────────────────────────────
numpy==2.0.2            # Numerical arrays (sentinel/anomaly.py feature engineering)
scikit-learn==1.9.0     # IsolationForest + StandardScaler + Pipeline (anomaly model)
joblib==1.5.3           # Model serialisation: load/save anomaly_model.joblib
